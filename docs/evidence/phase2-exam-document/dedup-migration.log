DEDUP MIGRATION EVIDENCE
Source file: prisma\migrations\20260813090000_exam_doc_dedup\migration.sql (contents read verbatim)
=================================================
-- Duplicate-document protection: a PDF is deduplicated within a single exam by
-- its SHA-256 content hash. The same file may still be uploaded for a
-- different exam (examId + fileHash scope).
CREATE UNIQUE INDEX "uploaded_exam_documents_exam_id_file_hash_key"
    ON "uploaded_exam_documents"("exam_id", "file_hash");
=================================================
Schema reference (prisma/schema.prisma, UploadedExamDocument):
@@unique([examId, fileHash])
=================================================
Interpretation of the constraint:
  same exam + same file_hash  -> UNIQUE violation -> duplicate rejected
  different exam + same file_hash -> allowed (scope is examId + fileHash)
